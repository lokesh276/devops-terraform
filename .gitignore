# Terraform state files (contain sensitive data)
terraform.tfstate
terraform.tfstate.backup
.terraform.tfstate.lock.info

# Terraform directory
.terraform/

# Private key (NEVER push this)
terra-key

# Lock file (optional)
.terraform.lock.hcl
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
